from agents.dynamicagent import DynamicAgent


class MinExpensesAgent(DynamicAgent):
    """
    this agent finds for each scooter the nearest nest.
    then it returns the vector that for each nests indicates how many scooters
    voted for this nest
    """
    pass
